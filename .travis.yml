language: python
python:
  - "3.8"
install:
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
  - pip install -r requirements.txt
  - pip install .
script:
  - pytest -sv
after_success:
  - codecov
